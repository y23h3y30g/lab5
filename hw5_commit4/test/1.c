/*
I'm level 1 test.
*/
void main() {
    int a, s;
    a = 0x11;
    s = 0;
    char ch;
    scanf("%d", &ch);
    while(a>0 && a<=10 || a%100==10 && !a==10) {
        a -= 1;
        int a;
        a = 10;
        s += a;
        if(-s < -10) {
            printf("result is: %d\n", s);
            int b;
            b = 10;
            for(int b=0; b<4; b++) {
                int w=10;
                printf("Have fun: %d\n", w);
            }
        }
    }
}
// No more compilation error.